const body=document.body;const toggle=document.querySelector("[data-nav-toggle]");const nav=document.querySelector("[data-nav]");toggle?.addEventListener("click",()=>body.classList.toggle("nav-open"));nav?.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>body.classList.remove("nav-open")));document.querySelectorAll("[data-year]").forEach(el=>el.textContent=new Date().getFullYear());const obs=new IntersectionObserver((entries)=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add("visible")}),{threshold:.14});document.querySelectorAll(".reveal").forEach(el=>obs.observe(el));